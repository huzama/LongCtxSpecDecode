"""Preliminary experiments: the calibration measurements behind the design doc
notes/work/exit-sparse-self-spec.md. Each module owns one measurement, has one
Config dataclass, and one run(cfg) entry; the CLI in main.py dispatches here.
"""
